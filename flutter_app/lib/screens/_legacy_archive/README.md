# Legacy Archive

Écrans et scripts legacy déplacés ici pour archivage. Ils ne sont plus
référencés dans le code actif.

| Fichier | Raison |
|---|---|
| `welcome_screen.dart` | Remplacé par `firebase_login_screen.dart` |
| `campagne_manage_screen.dart` | Fusionné dans `campagne_detail_screen.dart` |
| `player_character_main_screen.dart` | Obsolète, remplacé par `player_character_detail_screen.dart` |
| `main_test.dart` | Script de test alternatif, non référencé |
| `debug_templates.dart` | Script de debug Firestore, non référencé |
| `test_firebase.dart` | Script de test Firebase, non référencé |

> **Ne pas importer ces fichiers** — ils sont conservés uniquement comme
> référence historique. Supprimer ce dossier quand ils ne sont plus utiles.
